on: push

jobs:
  build:
    name: Run ESLint
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: 🧹 Run lint
        run: npm run lint
        with:
          rules-path: "./.eslintrc.js"
